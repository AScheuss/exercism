import java.util.stream.IntStream;

public class Pangrams {

	public static boolean isPangram(String string) {
		IntStream chars = string.chars();
		
		chars.filter((letter) -> string.contains(letter)).
			forEach(() -> );;
		return false;
	}

}
